spring:
  application:
    name: travel-account

  datasource:
    url: jdbc:mysql://localhost:3306/travel_account
    username: root
    password: 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.YH.travelaccount.domain
